# ปิดช่องโหว่ — ทำตามนี้ทีละขั้น (ห้ามข้าม)

ตอนนี้ใครก็ตามที่เปิดหน้าเว็บแล้วกดดูซอร์สโค้ด จะเห็น key ของ Supabase
แล้วเอาไป **อ่านและแก้ข้อมูลสต๊อก/ออเดอร์ทั้งหมด** ได้โดยไม่ต้องล็อกอิน

ไฟล์แอพเวอร์ชันใหม่มีหน้าล็อกอินแล้ว — เหลือแค่ตั้งค่าฝั่ง Supabase ให้ปฏิเสธคนที่ไม่ได้ล็อกอิน

> **ทำตามลำดับนี้เท่านั้น** ถ้าข้ามไปทำข้อ 4 ก่อน แอพจะใช้ไม่ได้ทั้งโรงงานทันที
> แนะนำให้ทำ **นอกเวลางาน** หรือตอนไม่มีกะเดิน

---

## ขั้นที่ 1 — สร้างบัญชีผู้ใช้ของโรงงาน (2 นาที)

1. เปิด https://supabase.com/dashboard เลือกโปรเจกต์ `tekhjdnbfncfvpwejlni`
2. เมนูซ้าย → **Authentication** → **Users** → ปุ่ม **Add user** → **Create new user**
3. กรอก
   - **Email**: อีเมลอะไรก็ได้ที่จำง่าย เช่น `staff@winpolydesign.com` (ไม่ต้องเป็นอีเมลจริง)
   - **Password**: ตั้งรหัสที่เดายาก **อย่างน้อย 12 ตัว** จดไว้ในที่ปลอดภัย
   - ติ๊ก **Auto Confirm User** ✅ (สำคัญ ไม่ติ๊กจะล็อกอินไม่ได้)
4. กด **Create user**

> จะสร้างหลายบัญชีก็ได้ เช่นแยกของออฟฟิศกับหน้าไลน์ ทำงานได้เหมือนกันทุกบัญชี

---

## ขั้นที่ 2 — อัปไฟล์แอพเวอร์ชันใหม่

อัป `stock-app_1.html` และ `factory-app-supabase.html` ตัวล่าสุดขึ้นโฮสต์ตามปกติ

**ตอนนี้แอพจะขึ้นหน้าล็อกอินแล้ว แต่ข้อมูลยังไม่ถูกล็อก** — เป็นช่วงเปลี่ยนผ่านที่ปลอดภัย
ถ้ามีอะไรผิดพลาด ย้อนกลับได้ทันทีโดยไม่กระทบข้อมูล

## ขั้นที่ 3 — ล็อกอินให้ครบทุกเครื่อง

ไล่เปิดแอพในทุกเครื่องที่ใช้งานจริง — คอมออฟฟิศ, แท็บเล็ตหน้าไลน์, มือถือทุกเครื่อง
ล็อกอินด้วยบัญชีจากขั้นที่ 1 **เครื่องละครั้งเดียว** ระบบจำไว้ให้ ไม่ต้องกรอกซ้ำอีก

ตรวจให้ครบว่าทุกเครื่องเข้าได้และเห็นข้อมูลปกติ **ก่อน**ไปขั้นต่อไป

## ขั้นที่ 4 — ล็อกข้อมูล (ขั้นสุดท้าย)

1. Supabase Dashboard → เมนูซ้าย → **SQL Editor** → **New query**
2. วางคำสั่งจากไฟล์ [`rls.sql`](rls.sql) ทั้งหมด → กด **Run**
3. ควรขึ้น `Success. No rows returned`

**ตรวจว่าปิดช่องโหว่ได้จริง**
- เปิดหน้าเว็บในโหมด **ไม่ระบุตัวตน / Incognito** → ต้องขึ้นหน้าล็อกอิน และเข้าไม่ได้ถ้าไม่มีรหัส
- กลับไปที่เครื่องที่ล็อกอินไว้แล้ว → ต้องใช้งานได้เหมือนเดิมทุกอย่าง

---

## ถ้ามีปัญหา — ย้อนกลับได้ทันที

เปิด SQL Editor แล้วรัน

```sql
alter table public.kv_store disable row level security;
```

ข้อมูลไม่หาย แอพกลับมาใช้ได้ทันทีเหมือนเดิม แล้วค่อยแจ้งผมมาดูให้

---

## เรื่องที่ควรรู้

**ล็อกอินบ่อยแค่ไหน** — เครื่องละครั้งเดียว ระบบต่ออายุให้เองเบื้องหลัง
จะถามใหม่ก็ต่อเมื่อล้างข้อมูลเบราว์เซอร์ หรือไม่ได้เปิดแอพนานมาก

**พนักงานลาออก / มือถือหาย** — เปลี่ยนรหัสผ่านที่ Supabase → Authentication → เลือก user → Reset password
แล้วให้เครื่องที่ยังใช้อยู่ล็อกอินใหม่ด้วยรหัสใหม่

**สั่งออกจากระบบเครื่องใดเครื่องหนึ่ง** — เปิดแอพ กด F12 → Console แล้วพิมพ์
```js
WPDAUTH.signOut()
```

**key ที่ฝังในหน้าเว็บยังอยู่** — ไม่เป็นไร หลังทำขั้นที่ 4 แล้ว key ตัวนั้นจะเปิดได้แค่หน้าล็อกอิน
อ่านข้อมูลอะไรไม่ได้เลยถ้าไม่มีรหัสผ่าน (นี่คือวิธีมาตรฐานของ Supabase)
