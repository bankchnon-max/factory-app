# รายการแก้ไข — WinPolyDesign (2569-08-20)

แก้จาก **ไฟล์เดิมของคุณ** โดยตรง (ค้นหาข้อความเดิม → แทนที่เฉพาะจุด) ไม่ได้เขียนใหม่
คีย์ข้อมูลและการเชื่อม Supabase ไม่ถูกแตะเลย → **ข้อมูลเดิมทั้งหมดยังอยู่ครบ**

| ไฟล์ | เดิม | หลังแก้ | ส่วนที่เหมือนเดิมแบบตัวต่อตัว |
|---|---|---|---|
| stock-app_1.html | 125,564 ตัวอักษร | 140,520 | หัวไฟล์ 12,898 + ท้ายไฟล์ 7,266 |
| factory-app-supabase.html | 154,030 ตัวอักษร | 159,116 | หัวไฟล์ 30,996 + ท้ายไฟล์ 34,321 |

---

## แอพสต๊อก — stock-app_1.html (แก้ 37 จุด)

### บั๊ก
| # | อาการ | สาเหตุจริงในโค้ดเดิม | แก้เป็น |
|---|---|---|---|
| 1.1 | เลือกไซซ์ในส่งเย็บ/รับกลับไม่ได้ เด้งกลับตัวแรกเสมอ | `rFabric()` สร้าง `<option>` ใหม่ทุกครั้งโดยไม่เก็บค่าที่เลือก และการเปลี่ยนไซซ์ก็ทำให้ `rFabric()` ทำงานอีกรอบ | เก็บค่าเดิมไว้ก่อนสร้าง option แล้วใส่กลับ + เพิ่มตัวช่วยกลางให้ `<select>` ทุกตัวจำค่าที่เลือกเสมอ |
| 1.2 | เลขล็อตใย/กระสอบซ้ำกันได้ | `nextFiberLot` / `nextSackNo` ใช้ **จำนวนแถว + 1** | เปลี่ยนเป็น **เลขสูงสุดที่มีอยู่ + 1** |
| 1.5 | "เหลือ (นิ้ว)" ของม้วนผ้าคลาดเคลื่อนหลายเท่า | `parseSize` คิดว่าไซซ์เป็น ซม. เสมอ แต่ป้ายกำกับไม่เคยบอก | คงค่าเริ่มต้นเป็น ซม. (ข้อมูลเดิมคำนวณเท่าเดิมเป๊ะ) + รองรับพิมพ์ `18.5x28 นิ้ว` ได้ + เขียนหน่วยกำกับที่ช่องกรอกทุกจุด |
| 1.6 | รับกลับเกินยอดส่งเย็บได้ → "ที่ช่างเย็บ" ติดลบ | เตือนแล้วยังบันทึกให้ | ห้ามบันทึกเกิน พร้อมบอกวิธีที่ถูก (ใช้โหมดกรอกอิสระ) |
| 1.7 | โหมดกรอกอิสระ "ส่งเย็บ" สร้างถุงใหม่ทับของจริง ยอด "ตัดได้" บวมเกิน | ไม่เคยเช็คว่ามีถุงจริงรออยู่ | เตือนพร้อมบอกเลขถุงและจำนวนที่รออยู่ ก่อนสร้างถุงใหม่ |
| 1.8 | ชนิดผ้า/ไซซ์ที่มาจากโหมดกรอกอิสระ ไม่โผล่ในดรอปดาวน์ เบิกไปผลิตไม่ได้ | `allFabricTypes()` / `allSizes()` อ่านจากตารางม้วนผ้าอย่างเดียว | รวมจาก `cutBatches` / `sewSent` / `sewReturns` ด้วย |
| 1.10 | ไม่มีปุ่มแก้ไขชื่อประเภทสินค้า | ไม่มีในโค้ดเดิมเลย | เพิ่มปุ่ม "✎ แก้ไขชื่อประเภท" — ย้ายข้อมูลส่งเย็บ/รับกลับ/จุดเตือนตามไปให้ครบ ถ้าชื่อซ้ำจะถามก่อนรวม |
| 1.11 | วันที่เริ่มต้นหน้าออกไฟล์เป็น 0026 และมีล็อตปี 0069 ในระบบ | เอาวันที่เก่าสุดมาใช้โดยไม่กรองปีเพี้ยน | กรองเฉพาะปี 2015–2100 + เพิ่มตัวกันกรอกปีผิดทุกช่องวันที่ (ถ้ากรอก พ.ศ. จะแปลงเป็น ค.ศ. ให้) |
| 1.13 | แท็บสรุปโชว์สินค้าสำเร็จรูป = 0 ทั้งที่มีของ | นับแค่ `finStock` ลืม `freeFinStock` (ยอดยกมาแบบอิสระ) | บวกทั้งสองเข้าด้วยกัน |
| 1.14 | "ใกล้หมด" ไม่รวมจุดเตือนของประเภทสินค้า + ขึ้นผ้าที่ไม่เคยมีม้วนรก ๆ | ตารางไล่เฉพาะใย/ผ้า/ถุง | เพิ่มประเภทสินค้า และตัดผ้าที่ไม่เคยรับม้วนออก |
| 1.16 | กดปุ่มบันทึกซ้ำระหว่างรอ → บันทึกซ้อน 2 รายการ | ไม่มีการกันกดซ้ำ | ปุ่มหยุดรับคลิกจนกว่าจะบันทึกเสร็จจริง + ป้าย "กำลังบันทึก…" กลางจอ |
| 1.3 | ลบ/แก้แล้วหายเงียบ ต้องรีเฟรชถึงรู้ว่าไม่ได้บันทึก | `flush()` ถ้าเจอว่ากำลังบันทึกคีย์เดิมอยู่ จะ `return` ทิ้งเงียบ ไม่ได้นัดลองใหม่ + หลังบันทึกเสร็จไล่ต่อแค่คีย์เดียว | นัดลองใหม่ทุก 0.4 วิ, ไล่บันทึกทุกคีย์ที่ค้าง, และเตือนก่อนปิด/รีเฟรชหน้าถ้ายังบันทึกไม่เสร็จ |

### จุดใช้งาน
- **2.2** กล่อง `confirm` / `alert` / `prompt` ของเบราว์เซอร์ → กล่องในหน้าเว็บทั้งหมด และคำถามลบบอกชัดว่าลบอะไร (ชื่อใย ล็อต น้ำหนัก วันที่)
- **2.3** ปุ่ม "แก้ไขยอด" → **"ปรับยอดคงเหลือ"** และเก็บประวัติการปรับไว้ใน `adjLog` ไม่ให้ยอดเดิมหายเงียบ
- **2.5** ดรอปดาวน์เลือกถุงบอกไซซ์ด้วย เช่น `ถุง #3 — 39*77 (เหลือส่ง 15 ชิ้น)`
- **2.6** เปลี่ยนแท็บแล้วเลื่อนขึ้นบนสุดให้
- **2.7** ไซซ์ถุง / น้ำหนักกระสอบ / น้ำหนักต่อใบ / ชื่อผู้เย็บ ค้างไว้หลังบันทึก
- **2.8** กด "+ เพิ่มไซซ์" แล้วไซซ์ล่าสุดค้างไว้ในแถวใหม่
- **2.9** ยังไม่เลือกชนิดผ้า จะขึ้น "— เลือกไซซ์ —" และ "— เลือกชนิดผ้าและไซซ์ก่อน —" แทนที่จะโชว์ไซซ์มั่ว

---

## แอพผลิต — factory-app-supabase.html (แก้ 8 จุด)

| # | อาการ | สาเหตุจริงในโค้ดเดิม | แก้เป็น |
|---|---|---|---|
| 1.4 | ปุ่ม "+ เพิ่มถุงซิลใบที่ 2" กดแล้วไม่มีอะไรเกิดขึ้นเลย | ปุ่มสั่ง `formDirty=true` **ก่อน** เรียก `buildSpecForm()` แต่บรรทัดแรกของฟังก์ชันคือ `if(formDirty && ...) return;` → ฟอร์มออกทันที ไม่เคยวาดช่องถุงใบที่ 2 | เก็บค่าที่พิมพ์ค้างไว้ลง `o.spec` ก่อน แล้วปลด `formDirty` ชั่วคราวเพื่อให้วาดใหม่ได้ (ปุ่มเอาถุงใบที่ 2 ออกก็แก้แบบเดียวกัน) |
| 1.12 | ประวัติเบิกกระสอบขึ้น `#88:undefinedกก.` | ตอนบันทึกเก็บเป็น `{sackNo, kg}` แต่ตอนแสดงผลอ่าน `p.take` | อ่าน `p.kg` (เผื่อข้อมูลเก่าที่เป็น `take` ไว้ด้วย) |
| 1.3 | ลบ/แก้แล้วหายเงียบ ต้องรีเฟรชถึงรู้ว่าไม่ได้บันทึก | `flush()` ถ้าเจอ `inflight` อยู่ จะ `return` เฉย ๆ ไม่ได้นัดลองใหม่ + หลังบันทึกเสร็จไล่ต่อแค่คีย์เดียว | นัดลองใหม่ทุก 0.4 วิ, ไล่บันทึกทุกคีย์ที่ค้าง, และ **เตือนก่อนปิด/รีเฟรชหน้าถ้ายังบันทึกไม่เสร็จ** |
| 1.3b | รายการเบิกใยของออเดอร์ที่ถูกลบไปแล้ว **มองไม่เห็นและลบไม่ได้ตลอดกาล** แต่ยังหักสต๊อกอยู่ → ยอดใยค้างติดลบถาวร | ตารางประวัติเบิกใยกรองด้วย `w.orderId===o.id` เท่านั้น | โชว์รายการที่ออเดอร์ถูกลบแล้วด้วย ติดป้าย "ออเดอร์ถูกลบแล้ว — ลบรายการนี้ได้เลย" กดลบได้ ยอดคืนเข้าสต๊อกทันที |
| 1.16 | กดปุ่มซ้ำระหว่างรอ | ไม่มีการกัน | ปุ่มหยุดรับคลิกจนบันทึกเสร็จ + ป้าย "กำลังบันทึก…" (วงกลมแตะป้อนใยเป็น `div` จึงไม่โดนกัน แตะรัวได้เหมือนเดิม) |
| — | `<select>` เด้งค่ากลับเวลารีเฟรชรายการ | เหมือนแอพสต๊อก | ตัวช่วยกลางให้จำค่าที่เลือกไว้ |
| — | กรอกปีผิดในช่องวันที่ | ไม่มีการตรวจ | ตัวช่วยกลางตัวเดียวกับแอพสต๊อก |

### หมายเหตุแก้รายงานเดิมของผม 2 ข้อ
อ่านโค้ดจริงแล้วพบว่า **ผมรายงานคลาดเคลื่อนไป 2 จุด** ขออภัยครับ:
- **2.10** ช่องเกณฑ์ยอมรับ **มีหน่วย % เขียนกำกับอยู่แล้ว** ("ยอมรับคลาดเคลื่อนได้ ± (%)") — ไม่ต้องแก้
- **2.11** ระบบ **มีแถบสีเหลือง "เตือน" อยู่แล้ว** ที่ช่วง 3–5% (ดีมาก ≤1% / ผ่าน ≤3% / เตือน ≤5% / เกินเกณฑ์ >5%) ตอนทดสอบผมบังเอิญไม่โดนช่วงนั้น — ไม่ต้องแก้

---

## วิธีเอาขึ้น
1. เอา `stock-app_1.html` ทับไฟล์เดิมบน Cloudflare Pages (path `stock-app_1.html`)
2. เอา `factory-app-supabase.html` ทับไฟล์เดิมของ production.winpolydesign.com
3. เปิดแอพแล้วกด Ctrl+F5 หนึ่งครั้ง (กันไฟล์เก่าค้างในแคช)

ไม่ต้องแตะฐานข้อมูล ไม่ต้อง migrate อะไรเลย — ข้อมูลเดิมอ่านได้ทันที



---

## ผลทดสอบการเชื่อมข้ามแอพ (ทดสอบจริงบนของที่อัปขึ้นแล้ว 20 ส.ค. 13:40–14:05)

สร้างของทดสอบในแอพสต๊อก → เบิกในแอพผลิต → กลับมาดูตัวเลขในแอพสต๊อก → ลบรายการเบิก → เช็คว่ายอดคืน

| รายการ | ก่อนเบิก | เบิก | หลังเบิก | หลังลบรายการเบิก |
|---|---|---|---|---|
| **ใย** TEST2-ใย ล็อต 1 | 100 กก. (เบิกใช้ 0) | 30 กก. | **70 กก. (เบิกใช้ 30)** ✓ | **กลับเป็น 100 กก.** ✓ |
| **ถุงบรรจุผ้า** ถุง #1 TEST2-ผ้า 20x30 | พร้อมผลิต 20 (เบิกแล้ว 0) | 8 ชิ้น | **พร้อมผลิต 12 (เบิกแล้ว 8)** ✓ | **กลับเป็น 20** ✓ |
| **กระสอบถุง** #1 TEST2-ถุง | 25 กก. (≈500 ใบ) | 7 กก. | **18 กก. (≈360 ใบ)** ✓ | **กลับเป็น 25 กก. (≈500 ใบ)** ✓ |

### หาของเจอไหม
- **เบิกใย** — พิมพ์ชื่อใยแล้วดรอปดาวน์ลอตขึ้น `ลอต 1 — รับ 100 กก. เหลือ 100 กก. (2026-08-20)` ✓ อ่านจากแอพสต๊อกจริง
- **เบิกถุงบรรจุผ้า** — เลือกชนิดผ้า/ไซซ์แล้วขึ้น `จะหยิบจาก: ถุง #1 (8 ชิ้น) รวม 8 ชิ้น ครบตามต้องการ` ✓ ไล่ FIFO จากถุงเก่าสุดให้เอง
- **เบิกกระสอบถุง** — ขึ้น `จะหยิบจาก: เบอร์กระสอบ #1 (7 กก.) รวม 7 กก. ครบตามต้องการ` ✓
- **ตั้งค่า Standard** — ชื่อถุงและเบอร์กระสอบจากสต๊อกจริงขึ้นให้เลือก ✓

### บั๊กที่ยืนยันว่าหายแล้วจากการทดสอบรอบนี้
- **1.1** เลือกไซซ์ 20x30 ในส่งเย็บ/รับกลับ → ค่าค้างอยู่ ไม่เด้งกลับ ✓
- **2.5** ดรอปดาวน์ถุงขึ้น `ถุง #1 — 20x30 (เหลือส่ง 20 ชิ้น)` มีไซซ์กำกับ ✓
- **2.9** ยังไม่เลือกชนิดผ้า ขึ้น `— เลือกชนิดผ้าและไซซ์ก่อน —` แทนไซซ์มั่ว ✓
- **1.12** ประวัติเบิกกระสอบขึ้น `#1:7 กก.` ไม่ใช่ `undefined` แล้ว ✓
- **1.3** ลบรายการเบิกแล้ว **บันทึกติดจริง** ยอดคืนเข้าสต๊อกครบทั้ง 3 อย่าง (รอบก่อนแก้ ลบแล้วเด้งกลับมา) ✓
- **2.2** กล่องยืนยันลบบอกรายละเอียดครบ เช่น `ลบรายการรับเข้าใยนี้? ใย: TEST2-ใย ล็อต: 1 น้ำหนัก: 100 กก.` ✓

ข้อมูลทดสอบ TEST2- ลบออกหมดแล้ว ไม่มีอะไรค้าง

---

# รอบที่ 2 — รื้อระบบ Standard ของหมอน (ถุงซิล + ถุงแพ็ค)
ไฟล์ที่แก้: `factory-app-supabase.html` (แอพผลิต) — แอพสต๊อกไม่ต้องแก้อะไรเพิ่ม

## ที่ขอมา
> "STANDARD ตอนทำหมอนมันจะใช้ถุงซิลกับถุงแพ็ค 1 แพ็คจะใช้ถุงซิล 10 ใบ ไม่ใช้ถุงซิลที่ 1 ที่ 2
> ต้องเป็นถุงซิลน้ำหนักเท่าไหร่ ใช้จำนวนเท่าไหร่ ถุงแพ็คน้ำหนักเท่าไหร่
> ต้องสามารถแท็กเบอร์กระสอบถุงแพ็คและกระสอบถุงซิลได้ด้วยเพื่อจะหักอัตโนมัติ ถ้าใช้ไม่หมดก็จะขึ้นจำนวนที่เหลือ"

## แก้อะไรไปบ้าง

### 1. เลิกใช้ "ถุงซิลใบที่ 1 / ใบที่ 2" — เปลี่ยนเป็น "น้ำหนักต่อใบ × จำนวนใบต่อแพ็ค"
- ตัด checkbox "ใช้ถุง 1 ใบต่อหมอน 1 ชิ้น" และปุ่ม "+ เพิ่มถุงใบที่ 2" ออก
- ฟอร์มใหม่ของหมอนเป็น 2 ก้อนชัดเจน:
  - **ถุงซิล** — ชื่อถุง · ไซซ์ · **น้ำหนักต่อใบ (กก.)** · **ใช้กี่ใบต่อ 1 แพ็ค** · **เบอร์กระสอบที่จะใช้**
  - **ถุงแพ็ค** — ชื่อถุง · **เบอร์กระสอบ** · **น้ำหนักต่อใบ (กก.)** · **ใช้กี่ใบต่อ 1 แพ็ค**
- Standard = (น้ำหนักหมอน × จำนวนชิ้น) + (ถุงซิล/ใบ × จำนวนใบ) + (ถุงแพ็ค/ใบ × จำนวนใบ)
  พรีวิวแยกบรรทัดให้เห็นทุกก้อน

### 2. ข้อมูลเก่าไม่หาย ไม่เพี้ยน
- ออเดอร์ที่ตั้งค่าไว้แบบเดิม (ถุงต่อชิ้น / ถุงใบที่ 2) **ค่า Standard เท่าเดิมเป๊ะทุกเคส** (ทดสอบไว้ด้านล่าง)
- ระบบแปลงให้เองเป็น "จำนวนใบต่อแพ็ค" แล้วขึ้นแถบเหลืองบอกว่า *ออเดอร์นี้ตั้งแบบเก่าไว้ — ตรวจตัวเลขอีกครั้งแล้วกดบันทึก*
- ค่าถุงใบที่ 2 เดิม (ชื่อ/น้ำหนัก/ไซซ์/เบอร์กระสอบ) ยังเก็บอยู่ในข้อมูล ไม่ถูกลบทิ้ง
- ถ้าตั้งจำนวนใบแบบใหม่แล้ว ถุงใบที่ 2 แบบเก่า **จะไม่ถูกนับซ้ำ**

### 3. แท็กเบอร์กระสอบ → หักสต๊อกอัตโนมัติ
- ช่องเบอร์กระสอบดึงจากสต๊อกจริง (`v3s_bag`) ขึ้นให้เลือกพร้อมยอดคงเหลือ เช่น
  `เบอร์กระสอบ #2 — เหลือ 20 กก. (1000 ใบ)`
- ทุกครั้งที่กดบันทึก 1 แพ็ค ระบบจะ **หักถุงซิลและถุงแพ็คออกจากกระสอบให้เอง**
  - หักจากกระสอบที่แท็กไว้ก่อน
  - ถ้ากระสอบนั้นหมดกลางคัน **ไหลไปกระสอบถัดไปให้เองตามลำดับเบอร์**
  - ถ้าสต๊อกไม่พอ หักเท่าที่มี แล้วเตือน `⚠ ถุงซิล ... ขาด 0.1 กก.`
- มี checkbox `หักถุงออกจากสต๊อกให้อัตโนมัติ` เผื่ออยากปิด (ปิดแล้วไม่หักเลย)

### 4. ขึ้นจำนวนที่เหลือ
- **Toast ตอนบันทึกแพ็ค** — `แพ็คที่ 1 · 10.28 กก. (ตาม Std) — ถุงซิล #2 เหลือ 990 ใบ · ถุงแพ็ค #1 เหลือ 2 ใบ`
- **การ์ดสรุป Standard** — `คงเหลือในกระสอบ: ถุงซิล ... กระสอบ #2 เหลือ 960 ใบ (19.2 กก.) · ถุงแพ็ค ... กระสอบ #1 เหลือ 0 ใบ`
- **การ์ดหน้าบันทึกไลน์** — โชว์บรรทัดเดียวกัน อัปเดตสดทุกแพ็ค
- แก้เพิ่ม: การ์ดสรุปเดิมไม่วาดใหม่หลังบันทึกแพ็ค ทำให้เลขค้าง — ตอนนี้วาดใหม่ทุกครั้งที่ไม่ได้กำลังพิมพ์แก้อยู่

### 5. ลบแพ็คแล้วคืนถุงเข้ากระสอบ (เจอเพิ่มตอนเทส)
- เดิมถ้าบันทึกแพ็คผิดแล้วลบทิ้ง ถุงที่หักไปแล้ว **ไม่คืน** สต๊อกจะเพี้ยนสะสม
- ตอนนี้รายการหักถุงอัตโนมัติผูกกับแพ็คนั้น (`lineId`) ลบแพ็ค = คืนถุงให้ทันที
- กล่องยืนยันบอกด้วยว่าจะคืนอะไรบ้าง: `ลบรายการผลิตนี้? ระบบจะคืนถุงที่หักไปให้ด้วย: ถุงซิล 0.2 กก. · ถุงแพ็ค 0.08 กก.`
- คืนเฉพาะรายการที่ **ระบบหักให้เอง** เท่านั้น รายการที่คนเบิกเองด้วยมือไม่ถูกแตะ

## ผลทดสอบ (34 เคส ผ่านหมด)
ทดสอบบนสำเนาแยก ไม่แตะข้อมูลจริงของโรงงานเลย

### ชุด A — ใช้งานจริงครบวงจร (18/18)
| # | ทดสอบ | ผล |
|---|---|---|
| 1-2 | ฟอร์มมีช่องใหม่ครบ / ไม่มีถุงใบที่ 1-2 กับ checkbox เก่าแล้ว | ✓ |
| 3-4 | เลือกเบอร์กระสอบถุงซิลและถุงแพ็คได้ ดึงยอดจากสต๊อกจริง | ✓ |
| 5 | คำนวณ 1×10 + 0.02×10 + 0.08×1 = **10.28 กก./แพ็ค** | ✓ |
| 6 | สรุปโชว์ถุงซิล/ถุงแพ็ค + เบอร์กระสอบ + คงเหลือ | ✓ |
| 7-9 | บันทึก 1 แพ็ค → หักถุงซิล 0.2 กก. จากกระสอบ #2, ถุงแพ็ค 0.08 กก. จากกระสอบ #1 อัตโนมัติ | ✓ |
| 10 | Toast บอกจำนวนที่เหลือ | ✓ |
| 11 | กระสอบถุงแพ็ค #1 หมด (0.24 กก.) → ไหลไปกระสอบ #2 เอง | ✓ |
| 12 | 4 แพ็ค หักถุงซิลรวม 0.8 กก. = 40 ใบ | ✓ |
| 13 | การ์ดทั้งสองใบอัปเดตเป็น "เหลือ 960 ใบ" | ✓ |
| 14-15 | **แอพสต๊อกเห็นยอดถูกหักจริง** — ถุงซิล 39.2 กก. ≈ 1960 ใบ, ถุงแพ็ค 7.92 กก. ≈ 99 ใบ | ✓ |
| 16-17 | ลบแพ็ค → คืนถุง 2 รายการ ยอดหักลดจาก 0.8 เหลือ 0.6 กก. | ✓ |
| 18 | ไม่มี JavaScript error | ✓ |

### ชุด B — เคสพิเศษ (5/5)
| # | ทดสอบ | ผล |
|---|---|---|
| 17 | ปิดหักอัตโนมัติ → ไม่หักสต๊อกเลย | ✓ |
| 18 | ถุงในกระสอบไม่พอ → หักเท่าที่มี + เตือน `ขาด 0.1 กก.` | ✓ |
| 19-20 | ออเดอร์ **ใย** ไม่มีช่องใหม่ และ Standard เท่าเดิม (10.02 กก.) | ✓ |
| 21 | ไม่มี JavaScript error | ✓ |

### ชุด C — ข้อมูลเก่าต้องไม่เพี้ยน (8/8)
| สเปคเดิม | Standard เดิม | Standard ใหม่ |
|---|---|---|
| ออเดอร์ใย ถุงใบเดียว | 1.05 | **1.05** ✓ |
| หมอน ถุงต่อชิ้น 1 ใบ | 10.2 | **10.2** ✓ |
| หมอน ถุงต่อชิ้น + ถุงใบที่ 2 | 10.5 | **10.5** ✓ |
| หมอน ถุงต่อแพ็ค + ถุงใบที่ 2 | 10.05 | **10.05** ✓ |
| หมอน + ถุงแพ็ค | 10.28 | **10.28** ✓ |
| ตั้งแบบใหม่ 10 ใบ + ถุงแพ็ค | — | 10.28 ✓ |
| ตั้ง 0 ใบ = ไม่บวกถุง | — | 10 ✓ |
| ตั้งแบบใหม่แล้ว ถุงใบที่ 2 เก่าไม่นับซ้ำ | — | 10.2 ✓ |

## หมายเหตุ
- คีย์ที่เก็บใน Supabase ไม่เปลี่ยน (`v3_orders`, `v3_feed`, `v3_line`, `v3_set`, `v3s_*`) ข้อมูลเดิมใช้ต่อได้ทันที
- แก้บนไฟล์เดิมทั้งหมด ไม่ได้เขียนใหม่

---

# รอบที่ 3 — แก้ "เบิกถุงบรรจุผ้า" เลือกอะไรไม่ได้เลย
ไฟล์ที่แก้: `factory-app-supabase.html`

## สาเหตุ (ตรวจจากข้อมูลจริงใน Supabase)
ดรอปดาวน์ **ชนิดผ้า** ดึงรายชื่อจาก "รายการรับเข้าผ้า" (`v3s_fabric.receipts`) อย่างเดียว
แต่ในระบบจริง **ไม่มีรายการรับเข้าผ้าเลยสักรายการ** (`receipts = 0`)
→ ดรอปดาวน์เลยว่างเปล่า กดเลือกอะไรไม่ได้ ทั้งที่มีถุงบรรจุผ้าตัดไว้ **213 ถุง** และพร้อมเบิกอยู่จริง **14,127 ชิ้น**

| ชนิดผ้า | พร้อมเบิก (ชิ้น) |
|---|---|
| ผ้าลายคละ | 2,748 |
| ผ้าขาว80g | 2,482 |
| S.P.B | 2,240 |
| ผ้าลายอังกฤษ | 1,618 |
| ผ้าลายดาว | 1,561 |
| ผ้าลายเป็ดเหลือง | 1,450 |
| ผ้าขาว100g | 760 |
| ผ้าข้าว80g | 743 |
| ผ้าริ้ว90g | 517 |
| ผ้าขาว80 | 8 |

## แก้อะไร
1. **ชนิดผ้าดึงจากทุกที่ที่มีชื่อผ้า** — รับเข้าผ้า + ถุงที่ตัดไว้ + ส่งเย็บ + รับกลับ
   ไม่ได้บันทึกรับเข้าผ้าก็ยังเบิกได้ตามปกติ
2. **ดรอปดาวน์บอกยอดพร้อมเบิกในตัวเลือกเลย** เช่น `ผ้าลายคละ — พร้อมเบิก 2748 ชิ้น` และเรียงของที่มีเยอะขึ้นก่อน
   ไซซ์ก็บอกเหมือนกัน `18x28 — พร้อมเบิก 160 ชิ้น`
3. **มีไซซ์เดียวเลือกให้เลย** ไม่ต้องกดซ้ำ
4. **บอกยอดตั้งแต่ยังไม่พิมพ์จำนวน** — `พร้อมเบิก 160 ชิ้น จาก 2 ถุง (#1:100, #2:60) — พิมพ์จำนวนที่ต้องการ`
   ยังไม่เลือกอะไรเลยก็บอกยอดรวมทั้งหมดให้ก่อน
5. **เบิกไม่ได้ต้องบอกสาเหตุ** แทนข้อความเดิม "ไม่มีถุงบรรจุผ้าที่พร้อม...เช็คในแอพสต๊อก" ซึ่งไม่บอกว่าให้เช็คอะไร
   - ตัดไว้แต่ยังไม่รับกลับ → `ผ้าลายดาว 17.5*26 ตัดไว้ 50 ชิ้น แต่ยังไม่ได้บันทึกรับกลับจากช่างเย็บ — ไปที่แอพสต๊อก แท็บผ้า → รับกลับจากช่างเย็บ`
   - เบิกไปหมดแล้ว → `รับกลับมาแล้ว X ชิ้น เบิกไปหมดแล้ว X ชิ้น — ไม่เหลือให้เบิก`
   - ยังไม่มีถุงเลย → บอกให้ไปตัดผ้าที่แอพสต๊อกก่อน
6. **ยอดอัปเดตทันทีที่เบิก** — เดิมยอดคงเหลือค้างจนกว่าจะ sync รอบถัดไป ทำให้เบิกซ้ำเกินของที่มีได้
   ตอนนี้คำนวณใหม่จากยอดเบิกล่าสุดทุกครั้ง
7. ใต้หัวข้อเพิ่มบรรทัดอธิบายเงื่อนไข: *ถุงจะขึ้นให้เบิกได้ก็ต่อเมื่อบันทึก "รับกลับจากช่างเย็บ" ในแอพสต๊อกแล้ว*

## ผลทดสอบ (10/10) — ใช้รูปข้อมูลเหมือนของจริง (ไม่มีรายการรับเข้าผ้าเลย)
| # | ทดสอบ | ผล |
|---|---|---|
| 1 | ดรอปดาวน์ชนิดผ้ามีของให้เลือก แม้ `receipts` ว่าง | ✓ |
| 2-3 | บอกยอดพร้อมเบิกในตัวเลือก และบอกยอดรวมก่อนเลือก | ✓ |
| 4 | ไซซ์ขึ้นให้เลือก + มีไซซ์เดียวเลือกให้เอง | ✓ |
| 5 | บอกว่าพร้อมเบิกกี่ชิ้นจากถุงไหน ก่อนพิมพ์จำนวน | ✓ |
| 6 | ไล่หยิบ FIFO ถุง #1 100 + ถุง #2 50 = 150 ครบ | ✓ |
| 7 | กดเบิกแล้วบันทึกจริง หักถูกถุง | ✓ |
| 8 | ยอดพร้อมเบิกลดทันทีหลังเบิก | ✓ |
| 9 | ถุงที่ยังไม่รับกลับ → บอกสาเหตุชัดเจน | ✓ |
| 10 | ไม่มี JavaScript error | ✓ |

รันชุดเทสรอบที่ 2 ซ้ำอีกครั้ง (31 เคส) ผ่านหมดเหมือนเดิม

## ข้อสังเกตจากข้อมูลจริง (ไม่ใช่บั๊ก แต่ควรจัดการ)
- ชื่อไซซ์พิมพ์ไม่ตรงกันในบางรายการ ทำให้ระบบมองเป็นคนละไซซ์ เช่น
  `12.5*45` กับ `12.45` · `18x28` กับ `18*28` · ชนิดผ้า `ผ้าขาว80` กับ `ผ้าขาว80g`
  ตอนนี้เห็นยอดแยกกันในดรอปดาวน์แล้ว ถ้าอยากรวมต้องไปแก้ชื่อในแอพสต๊อก
- เลขถุง (batchNo) ซ้ำกันหลายใบในผ้าชนิด/ไซซ์เดียวกัน (มี #1 หลายใบ) — การเบิกยังหักถูกใบเพราะอิงรหัสภายใน แต่ดูด้วยตาจะสับสน

---

# รอบที่ 4 — รวมชื่อผ้า/ไซซ์ที่เขียนไม่ตรงกัน ให้เป็นข้อมูลชุดเดียว
ไฟล์ที่แก้: `stock-app_1.html` (แอพสต๊อก) + แก้ข้อมูลจริงใน Supabase

## 1. แก้ข้อมูลจริงแล้ว (ทำครั้งเดียวจบ)
สำรองข้อมูลเดิมไว้ก่อนที่คีย์ `v3s_fabric__backup_20260820` (กู้คืนได้ตลอด) และดาวน์โหลดไฟล์
`v3s_fabric-backup-ก่อนรวมชื่อ-25690820.json` เก็บไว้อีกชุด

| แก้จาก | เป็น | จำนวนรายการที่ย้าย |
|---|---|---|
| ชนิดผ้า `ผ้าข้าว80g` | `ผ้าขาว80g` | 24 |
| ไซซ์ `18x28` | `18*28` | รวม 25 |
| ไซซ์ `12.45` | `12.5*45` | (รวมในข้างบน) |

- `ผ้าขาว80` (ไม่มี g) **ไม่ได้แตะ** ตามที่สั่ง
- `33cm*151cm` **ไม่ได้แตะ** ตามที่สั่ง
- ตรวจก่อน-หลังแล้ว ยอดเท่าเดิมทุกตัว: ตัด 15,109 ชิ้น · ส่งเย็บ 12,168 ชิ้น · รับกลับ 14,127 ชิ้น ·
  ถุง 213 ใบ · รหัสถุงครบเท่าเดิม — **ไม่มีข้อมูลหายแม้แต่รายการเดียว**
- กลุ่มชนิดผ้า+ไซซ์ ลดจาก 24 เหลือ 22 กลุ่ม (คือรวมสำเร็จ 2 คู่)
- เขียนสัญญาณ sync ให้แล้ว เครื่องที่เปิดแอพค้างไว้จะอัปเดตเองภายในไม่กี่วินาที

## 2. เพิ่มเครื่องมือในแอพสต๊อก — แก้เองได้ ไม่ต้องรอผม
แท็บ **ผ้า** เพิ่ม 2 อย่าง (อยู่ใต้ "แก้ไขชื่อประเภทสินค้า")
- **แก้ไขชื่อชนิดผ้า** — เลือกชื่อที่พิมพ์ผิด แล้วพิมพ์ชื่อที่ถูก ข้อมูลเก่าย้ายตามทั้งหมด
- **แก้ไขไซซ์** — เช่น `18x28` → `18*28`
- ก่อนแก้จะบอกก่อนว่าจะย้ายกี่รายการ และมาจากไหนบ้าง เช่น
  `จะย้ายข้อมูลตามไปด้วย 3 รายการ (ถุงที่ตัดไว้ 1, ส่งเย็บ 1, รับกลับ 1)`
- ถ้าพิมพ์ชื่อตรงกับของที่มีอยู่แล้ว จะถามยืนยันว่าจะ**รวมเป็นชุดเดียวกัน** พร้อมย้ำว่ายอดยังอยู่ครบ
- ครอบคลุมทุกที่ที่เก็บชื่อไว้: รับเข้าผ้า · ตัดผ้า (รวมไซซ์ในแต่ละแถว) · ถุงที่ตัดไว้ · ส่งเย็บ · รับกลับ · ของเสีย

## 3. เตือนอัตโนมัติเมื่อมีชื่อที่น่าจะซ้ำ
ในแท็บผ้าจะขึ้นแถบเตือนเองเมื่อเจอชื่อที่ต่างกันแค่วิธีเขียน
(ไม่สนตัวพิมพ์เล็กใหญ่ · ช่องว่าง · `x` `×` `*` · `cm` `นิ้ว` `มม`) เช่น
`น่าจะเป็นอันเดียวกันแต่เขียนคนละแบบ: 18*28 / 18x28 — แก้ให้เหมือนกันแล้วยอดจะรวมเป็นชุดเดียว`
ถ้าไม่มีที่ซ้ำจะขึ้นแถบเขียว `ชื่อผ้าและไซซ์ไม่มีที่เขียนซ้ำกัน`

## ผลทดสอบ (10/10)
| # | ทดสอบ | ผล |
|---|---|---|
| 1 | มีเครื่องมือแก้ชื่อชนิดผ้าและไซซ์ในแท็บผ้า | ✓ |
| 2 | เตือนอัตโนมัติว่าอันไหนน่าจะเป็นอันเดียวกัน | ✓ |
| 3 | บอกก่อนว่าจะย้ายข้อมูลตามไปกี่รายการ จากไหนบ้าง | ✓ |
| 4 | เตือนว่าจะรวมเข้ากับของที่มีอยู่แล้ว + ยืนยันว่าข้อมูลไม่หาย | ✓ |
| 5 | ไซซ์รวมเป็นอันเดียว ไม่เหลือของเก่า | ✓ |
| 6 | ส่งเย็บ/รับกลับ ย้ายตามไปด้วย | ✓ |
| 7 | ยอดครบเท่าเดิม (ถุง 3 · ตัด 240 · รับกลับ 160) | ✓ |
| 8 | แก้ชื่อชนิดผ้าเป็นชื่อใหม่ได้ | ✓ |
| 9 | แก้เสร็จแล้วคำเตือนหายไปเอง | ✓ |
| 10 | ไม่มี JavaScript error | ✓ |

รันชุดเทสรอบก่อนหน้าซ้ำทั้งหมด (41 เคส) ผ่านหมดเหมือนเดิม

---

# รอบที่ 5 — เปลี่ยนตัวคูณเป็น x ทั้งระบบ
ไฟล์ที่แก้: `stock-app_1.html` + `factory-app-supabase.html` + ข้อมูลจริงใน Supabase

ตัวคูณมาตรฐานคือ **x** (เช่น `18x28`) — เดิมในระบบมีทั้ง `*` `×` `X` ปนกัน ทำให้ของอย่างเดียวกันถูกนับเป็นคนละอย่าง

## 1. แก้ข้อมูลจริงแล้ว — 890 จุด
สำรองไว้ก่อนทุกคีย์ที่ `<ชื่อคีย์>__backup_x20260820` และโหลดเป็นไฟล์
`backup-ก่อนเปลี่ยนตัวคูณเป็น-x-25690820.txt` เก็บไว้อีกชุด

| คีย์ | เปลี่ยนอะไร |
|---|---|
| `v3s_fabric` | ไซซ์ถุงบรรจุผ้า + ชื่อประเภทสินค้า เช่น `12.5*45` → `12.5x45`, `หมอนหนุนริ้ว18*26.5` → `หมอนหนุนริ้ว18x26.5` |
| `v3s_bag` | ชื่อ/ไซซ์กระสอบถุง เช่น `70*130` → `70x130`, `70+10*100` → `70+10x100` |
| `v3_orders` | รหัส/สเปคงาน เช่น `ใย 300*34*46` → `ใย 300x34x46` |
| คีย์อื่น | ตรวจแล้วไม่มีตัวคูณ `*` |

ตรวจก่อน-หลังทุกคีย์: โครงสร้างข้อมูลเท่าเดิมทุกชิ้น (นับ node ตรงกันหมด 9 คีย์)
ยอดคงเดิม — ตัด 15,109 ชิ้น · รับกลับ 14,127 ชิ้น · ถุงบรรจุผ้า 213 ใบ · กระสอบถุง 207 ใบ
เหลือ `*` ที่เป็นตัวคูณในระบบ **0 จุด**

## 2. ต่อไปพิมพ์ `*` มาก็แปลงให้เอง (ทั้ง 2 แอพ)
เพิ่มตัวแปลง `normSize()` ทำงานตอนกดบันทึก — พิมพ์ `*` `×` `X` หรือเว้นวรรค (`18 * 28`) มา
ระบบเก็บเป็น `18x28` ให้เสมอ จะได้ไม่มีของซ้ำเพราะเขียนคนละแบบอีก

จุดที่แปลงให้:
- **แอพสต๊อก** — ไซซ์ตอนตัดผ้า · ไซซ์ตอนส่งเย็บ/รับกลับแบบอิสระ · ชื่อและไซซ์กระสอบถุง ·
  ชื่อประเภทสินค้า (ส่งเย็บ/รับกลับ) · ช่องแก้ไขชื่อผ้า/ไซซ์/ประเภทสินค้า
- **แอพผลิต** — รหัส/สเปคงานตอนสร้างออเดอร์ · ชื่อและไซซ์ถุงซิล · ชื่อถุงแพ็ค · ไซซ์ผ้าใน Standard · ขนาดถุงตอนบันทึกไลน์

แปลงเฉพาะตอนที่เป็นตัวคูณจริง (มีตัวเลขตามหลัง) — `หมายเหตุ 3*` ไม่ถูกแตะ

## 3. แถบเตือนของซ้ำเปลี่ยนมาตรฐานเป็น x ด้วย
ยังเตือนเหมือนเดิมถ้าเจอชื่อที่ต่างกันแค่วิธีเขียน แต่ตอนนี้ยึด `x` เป็นหลัก

## ผลทดสอบ
- **normSize 14 เคส** — `18*28`→`18x28` · `18 * 28`→`18x28` · `18×28` · `18X28` · `40 x 70`→`40x70` ·
  `33cm*151cm`→`33cmx151cm` · `ใย 300*34*46`→`ใย 300x34x46` · `70+10*100`→`70+10x100` ·
  `หมายเหตุ 3*` ไม่เปลี่ยน · `12.45` ไม่เปลี่ยน · `S.P.B` ไม่เปลี่ยน — ผ่านหมด
- **เครื่องมือแก้ชื่อ 10/10** — พิมพ์ `18*28` เข้าไป ระบบแปลงเป็น `18x28` แล้วถามยืนยันรวมกับของเดิมให้ถูกต้อง
- รันชุดเทสรอบก่อนหน้าซ้ำทั้งหมด (41 เคส) ผ่านหมดเหมือนเดิม

---

# รอบที่ 6 — ตัวกันพิมพ์ผิด + ทำให้ใช้บนมือถือได้จริง
ไฟล์ที่แก้: `stock-app_1.html` + `factory-app-supabase.html`

## 1. ตัวกันพิมพ์ผิด — กันสต๊อกหายแบบไม่มีใครรู้
ความเสี่ยงที่ใหญ่ที่สุดคือพิมพ์ `0.2` แทน `0.02` แล้วระบบหักถุงผิด 10 เท่าทุกแพ็ค
ตอนนี้ระบบเทียบตัวเลขกับของจริงในสต๊อกให้ ก่อนจะยอมบันทึก

### แอพผลิต — ตอนตั้ง Standard
เตือน **สดตั้งแต่กำลังพิมพ์** (ขึ้นในกล่องพรีวิว) และ **ถามยืนยันอีกครั้งตอนกดบันทึก**
- น้ำหนักถุงต่างจากที่บันทึกไว้ในแอพสต๊อกเกิน 3 เท่า
  `ถุงซิล ที่กรอก 0.2 กก./ใบ ต่างจากที่บันทึกไว้ในแอพสต๊อกมาก (สต๊อกบันทึก 0.02 กก./ใบ)`
- ถุง 1 ใบหนักเกิน 5 กก.
- น้ำหนักถุงรวมมากกว่าครึ่งหนึ่งของน้ำหนักหมอน
- น้ำหนักต่อชิ้นต่ำกว่า 0.005 หรือเกิน 30 กก. · ชิ้นต่อแพ็คเกิน 300 · ถุงซิลเกิน 200 ใบ/แพ็ค · ถุงแพ็คเกิน 50 ใบ/แพ็ค

กล่องยืนยันบอกเหตุผลด้วยว่าทำไมต้องระวัง:
*"ระบบจะหักถุงออกจากสต๊อกตามตัวเลขนี้ทุกแพ็ค ถ้าผิดสต๊อกจะเพี้ยนสะสม"*
กดยกเลิก = ไม่บันทึกอะไรเลย · ตัวเลขปกติ = บันทึกผ่านเลย ไม่ถามซ้ำซาก

### แอพสต๊อก — ตอนรับเข้า
- **รับเข้ากระสอบถุง**: ถุง 1 ใบหนักกว่าทั้งกระสอบ · ถุง 1 ใบเกิน 5 กก. · กระสอบได้ถุงน้อยกว่า 3 ใบ
  หรือเกิน 200,000 ใบ · กระสอบหนักเกิน 200 กก. · ต่างจากครั้งก่อนของถุงชื่อเดียวกันเกิน 3 เท่า
  `ครั้งก่อนถุง "ถุงซิล WD" บันทึกไว้ 0.02 กก./ใบ แต่ครั้งนี้กรอก 2 กก./ใบ`
- **รับเข้าใย**: เกิน 20,000 กก. · น้อยกว่า 1 กก. · ต่างจากครั้งก่อนของใยตัวเดียวกันเกิน 10 เท่า ·
  เฉลี่ยก้อนละเกิน 500 กก.

## 2. ใช้บนมือถือได้จริง
ตรวจด้วยจอมือถือจริง (393x727) ทุกหน้าในแอพสต๊อก (ใย/ผ้า/ถุง/แก๊ส/สินค้า/จัดส่ง/สรุป) และแอพผลิต

เจอปัญหาแล้วแก้:
- **ช่องกรอกเตี้ยเกินกดยาก** — เดิม 36px, ดรอปดาวน์ 38px (มาตรฐานคือ 44px)
  → ใส่ `min-height:44px` และขยายตัวหนังสือเป็น 16px เฉพาะจอ ≤640px
  (16px สำคัญ เพราะ iPhone จะซูมหน้าจอเองถ้าตัวหนังสือเล็กกว่านี้ตอนแตะช่องกรอก)
- **ปุ่มลบ ✕ กับปุ่มแก้ ✎ ในตาราง เล็กมาก** — 22-23px กดพลาดง่ายมาก โดยเฉพาะปุ่มลบ
  → ขยายเป็นอย่างน้อย 40x40px บนมือถือ
- ตรวจแล้วไม่มีอะไรล้นขอบจอ ตารางกว้างๆ เลื่อนในกรอบตัวเองถูกต้อง หน้าจอไม่เลื่อนซ้ายขวา

## ผลทดสอบ
### ตัวกันพิมพ์ผิด (11/11)
| # | ทดสอบ | ผล |
|---|---|---|
| 1 | พิมพ์ 0.2 แทน 0.02 → เตือนสดในพรีวิวทันที บอกว่าสต๊อกบันทึก 0.02 | ✓ |
| 2 | กดบันทึก → ถามยืนยัน พร้อมบอกว่าสต๊อกจะเพี้ยนสะสม | ✓ |
| 3 | กดยกเลิก → ไม่บันทึกอะไรเลย | ✓ |
| 4-5 | แก้เป็น 0.02 → คำเตือนหาย บันทึกผ่านเลย ไม่ถามซ้ำ | ✓ |
| 6 | ถุงรวมหนักเกินครึ่งของหมอน → เตือน | ✓ |
| 7-8 | รับเข้ากระสอบ 2 กก./ใบ (ครั้งก่อน 0.02) → เตือน + ยกเลิกแล้วไม่บันทึก | ✓ |
| 9 | ค่าปกติ → บันทึกผ่านเลย | ✓ |
| 10 | รับเข้าใย 5,000 กก. (ครั้งก่อน 100) → เตือน | ✓ |
| 11 | ไม่มี JavaScript error | ✓ |

### มือถือ (16/16)
- แอพสต๊อกทั้ง 7 หน้า + แอพผลิต: ไม่ล้นขอบจอ · ปุ่ม/ช่องกรอก ≥40px · ตัวหนังสือ ≥11px · ไม่มี error
- **ใช้งานจริงบนมือถือด้วยการแตะและพิมพ์จริง** (ไม่ได้ยัดค่าเข้าไปตรงๆ):
  ตั้ง Standard ครบทุกช่อง → บันทึกแพ็ค → หักถุงอัตโนมัติ ขึ้น
  `แพ็คที่ 1 · 10.28 กก. (ตาม Std) — ถุงซิล #1 เหลือ 990 ใบ · ถุงแพ็ค #1 เหลือ 124 ใบ`
  → เบิกถุงบรรจุผ้า 40 ชิ้นสำเร็จ
- จำลองแป้นพิมพ์เด้งขึ้นมา (จอเตี้ยลงเหลือครึ่งเดียว) ยังเลื่อนไปกดปุ่มบันทึกได้

### รวมทุกชุด — 62 เคส ผ่านหมด
`ข้อมูลเก่าไม่เพี้ยน 8` · `Standard หมอน 18` · `เคสพิเศษ 5` · `เบิกถุงบรรจุผ้า 10` ·
`แก้ชื่อผ้า/ไซซ์ 10` · `ตัวกันพิมพ์ผิด 11` (+ มือถือ 16 เคส)

---

# รอบที่ 7 — ก้อนใย: แท็กก้อนที่ใช้จริง + หักใยอัตโนมัติทุกรอบป้อน
ไฟล์ที่แก้: `factory-app-supabase.html`

## ปัญหาของเดิม (ตรวจจากโค้ด)
ช่อง **"เบอร์แท็กก้อนที่ใช้ (ถ้ามี)"** ที่อยู่ในแถวสูตร **ใช้งานจริงไม่ได้**
- มันเก็บอยู่กับ *สูตร* ไม่ใช่กับ *รอบป้อนใย* → ทั้งออเดอร์มีได้ค่าเดียว เปลี่ยนก้อนแล้วทับของเก่าทิ้ง
  บอกไม่ได้ว่า "รอบที่ 5 ใช้ก้อนไหน" ซึ่งเป็นสิ่งที่คำอธิบายใต้ช่องอ้างว่าทำได้
- **ไม่หักสต๊อกเลย** ไม่โผล่ที่ไหนนอกจากไฟล์ export
- ของที่หักสต๊อกจริงคือการ์ด "เบิกใย (เปลี่ยนก้อนใย)" ซึ่งมีช่อง "หมายเลขก้อน" อยู่แล้ว
  → พนักงานต้องพิมพ์เบอร์ก้อน **2 ที่** แต่มีที่เดียวที่นับจริง

## แก้เป็นแบบใหม่ (ทำทั้ง 2 อย่างตามที่สั่ง)

### 1. การ์ดใหม่ "ก้อนใยที่กำลังใช้อยู่" (หน้าป้อนใย)
โชว์ใยทุกตัวในสูตร พร้อมกัน 3 อย่างในบรรทัดเดียว
`TEST-ใยA  4 กก./รอบ · ก้อนที่ใช้อยู่ #7 · คงเหลือในสต๊อก 106 กก. (ล็อต 1 เหลือ 6 กก.)`
- ยังไม่แท็ก → ขึ้นป้ายเหลือง `ยังไม่ได้แท็กเบอร์ก้อน`
- ใยหมดสต๊อก → ขึ้นป้ายแดง

### 2. ปุ่ม 🔄 เปลี่ยนก้อน — กดทีเดียวจบ
- กดข้างชื่อใยตัวที่เปลี่ยน → พิมพ์เบอร์แท็กก้อนใหม่ → จบ
- **เดาเบอร์ถัดไปให้เลย** (ก้อน #7 → เสนอ 8) แค่กดยืนยัน
- บอกชัดว่า *"รอบป้อนใยหลังจากนี้จะถูกบันทึกว่าใช้ก้อนนี้"*
- **เปลี่ยนก้อนแล้วแท็กเปลี่ยนตามทันที** — รอบเก่ายังผูกกับ #7 รอบใหม่ผูกกับ #8 ย้อนดูได้ว่าก้อนไหนใช้ไปกี่กิโล

### 3. หักใยอัตโนมัติทุกครั้งที่แตะ +1 รอบ
- แตะวงกลม +1 ครั้ง = หักใยตามสูตรออกจากสต๊อกให้เอง ไล่จากล็อตเก่าสุดก่อน
- ล็อตเก่าหมดกลางคัน **ไหลไปล็อตถัดไปเองอัตโนมัติ**
- รายการที่หักผูกกับเบอร์ก้อนที่แท็กไว้ทุกใบ
- Toast บอกยอดคงเหลือทันที `รอบที่ 1 — ใยA เหลือ 106 กก. · ใยB เหลือ 49 กก.`
  ใยไม่พอ → เตือน `⚠ ใยA ขาด 2 กก.` · ยังไม่แท็กก้อน → เตือนต่อท้าย
- มี checkbox ปิดได้ ถ้าอยากเบิกเองแบบเดิม

### 4. ลบรอบป้อนใย = คืนใยเข้าสต๊อก
เหมือนที่ทำกับถุงในรอบก่อน — กดผิดแล้วลบ ใยกลับเข้าสต๊อกทันที
กล่องยืนยันบอกก่อนว่าจะคืนอะไร `ระบบจะคืนใยที่หักไปให้ด้วย: ใยA 4 กก. · ใยB 1 กก.`
คืนเฉพาะที่ระบบหักเอง ไม่แตะรายการที่คนเบิกเองด้วยมือ

### 5. ช่องแท็กเก่าในสูตร ซ่อนไปแล้ว
ไม่ลบข้อมูล — ค่าเก่าที่เคยพิมพ์ไว้ถูกใช้เป็น "ก้อนเริ่มต้น" ให้อัตโนมัติ
ใต้แถวสูตรเปลี่ยนข้อความเป็น *"แท็กที่การ์ด ก้อนใยที่กำลังใช้อยู่ ด้านบน"*

## ผลทดสอบ (16/16)
| # | ทดสอบ | ผล |
|---|---|---|
| 1-2 | การ์ดโชว์ใยครบ + ยอดคงเหลือ + เตือนเมื่อยังไม่แท็ก | ✓ |
| 3-4 | กดเปลี่ยนก้อน → พิมพ์เบอร์ → โชว์ #7 / #3 | ✓ |
| 5 | แตะ +1 → หักใยA 4 กก. ใยB 1 กก. ตามสูตร | ✓ |
| 6 | รายการที่หักผูกกับเบอร์ก้อนถูกตัว | ✓ |
| 7 | Toast บอกคงเหลือ 106 / 49 กก. | ✓ |
| 8 | ล็อต 1 หมด (10 กก.) ไหลไปล็อต 2 เอง | ✓ |
| 9 | เปลี่ยนก้อนเดาเบอร์ถัดไปให้ (#7 → 8) | ✓ |
| 10-11 | เปลี่ยนก้อนแล้วแท็กเปลี่ยนตาม ของเก่ายังเป็น #7 · ใยตัวอื่นไม่กระทบ | ✓ |
| 12-13 | ลบรอบ → บอกก่อนว่าจะคืนเท่าไหร่ แล้วคืนจริง | ✓ |
| 14 | ปิดหักอัตโนมัติ → ไม่หักสต๊อก | ✓ |
| 15 | ช่องแท็กเก่าในสูตรไม่โผล่ให้สับสนแล้ว | ✓ |
| 16 | ไม่มี JavaScript error | ✓ |

รันชุดเทสเดิมซ้ำทั้งหมด (62 เคส) ผ่านหมด — **รวมตอนนี้ 78 เคส**

---

# รอบที่ 8 — ปิดช่องโหว่: ต้องล็อกอินก่อนถึงจะเข้าถึงข้อมูลได้
ไฟล์ที่แก้: `stock-app_1.html` + `factory-app-supabase.html` + เพิ่ม `SECURITY-SETUP.md`, `rls.sql`

## ปัญหา
key ของ Supabase ฝังอยู่ในหน้าเว็บที่เปิดสาธารณะ ใครกดดูซอร์สโค้ดก็เอาไป
**อ่านและแก้ข้อมูลสต๊อก/ออเดอร์ทั้งหมดได้** โดยไม่ต้องล็อกอิน
(ยืนยันแล้วจากการทดสอบจริง — อ่านและเขียนข้อมูลได้ด้วย key ที่หยิบจากหน้าเว็บ)

## แก้อะไร

### ฝั่งแอพ — หน้าล็อกอิน + ส่ง token ทุกคำขอ
- เปิดแอพครั้งแรกต้องล็อกอินก่อน ยังไม่ล็อกอิน = อ่านข้อมูลไม่ได้เลยแม้แต่คำขอเดียว
- ล็อกอิน **ครั้งเดียวต่อเครื่อง** ระบบต่ออายุให้เองเบื้องหลัง ไม่ต้องกรอกซ้ำทุกวัน
- token หมดอายุหรือถูกยกเลิก → ต่ออายุให้เอง ถ้าต่อไม่ได้ก็เด้งหน้าล็อกอินแล้วทำงานต่อ ไม่พังเงียบ
- ข้อความผิดพลาดเป็นภาษาไทย (`อีเมลหรือรหัสผ่านไม่ถูกต้อง`)
- ช่องกรอกขนาดมือถือ 16px กันจอซูมเอง
- สั่งออกจากระบบได้ด้วย `WPDAUTH.signOut()`

### ฝั่ง Supabase — ต้องทำเอง 4 ขั้น
ดู **[SECURITY-SETUP.md](SECURITY-SETUP.md)** เขียนไว้ละเอียดทีละขั้น พร้อม `rls.sql` ที่คัดลอกไปวางได้เลย
ลำดับสำคัญมาก: สร้างบัญชี → อัปไฟล์ → ล็อกอินให้ครบทุกเครื่อง → **ค่อย**เปิด RLS
ทำสลับลำดับแอพจะใช้ไม่ได้ทั้งโรงงาน — และมีคำสั่งย้อนกลับบรรทัดเดียวถ้ามีปัญหา

## ผลทดสอบ (20/20)
จำลองเซิร์ฟเวอร์ที่ **เปิด RLS แล้ว** (ปฏิเสธทุกคำขอที่ไม่มี token ผู้ใช้)

| # | ทดสอบ (ทำทั้ง 2 แอพ) | ผล |
|---|---|---|
| 1 | เปิดแอพครั้งแรก → ต้องล็อกอินก่อน ใช้งานไม่ได้ทันที | ✓ |
| 2 | ยังไม่ล็อกอิน → อ่านข้อมูลสำเร็จ **0 ครั้ง** | ✓ |
| 3 | รหัสผิด → บอกเป็นไทยว่าไม่ถูกต้อง และยังไม่ให้เข้า | ✓ |
| 4-5 | รหัสถูก → เข้าได้ อ่านข้อมูลสำเร็จ 18 ครั้ง | ✓ |
| 6 | ข้อมูลขึ้นครบหลังล็อกอิน | ✓ |
| 7 | เปิดใหม่ในเครื่องเดิม → ไม่ถามซ้ำ | ✓ |
| 8 | เครื่องอื่นที่ไม่เคยล็อกอิน → เห็นข้อมูลไม่ได้ | ✓ |
| 9-10 | token หมดอายุ + ต่ออายุไม่ผ่าน → เด้งล็อกอินใหม่ แล้วใช้งานต่อได้ | ✓ |
| 11 | สั่งออกจากระบบ → กลับหน้าล็อกอิน | ✓ |
| 12 | ไม่มี JavaScript error | ✓ |

รันชุดเทสเดิมซ้ำทั้งหมด ผ่านหมด — **รวมตอนนี้ 114 เคส**

---

# รอบที่ 9 — สูตรใย: เลือกเบอร์ก้อนจากสต๊อก + ก้อนหมดสลับให้เองพร้อมประวัติ
ไฟล์ที่แก้: `factory-app-supabase.html`

## ปัญหาที่เจอจากคำถาม "ถ้าใยในสูตรหมด จะหักก้อนต่อไปคือใยที่เบิกใช่มั้ย"
ตรวจแล้วพบ 2 เรื่อง

1. **ระบบไม่ได้หักเป็น "ก้อน" แต่หักเป็นกิโลออกจาก "ล็อต"** — เบอร์ก้อนที่แท็กไว้เป็นแค่ป้ายกำกับ
   ไม่ได้ผูกกับจำนวนกิโล ระบบจึงไม่มีทางรู้ว่าก้อนหมดเมื่อไหร่
2. **ยอดหักซ้ำ** — การเบิกใยด้วยมือกับการหักอัตโนมัติหักจากกองเดียวกัน
   ทดสอบจริง: ใย 100 กก. ป้อน 1 รอบ (4 กก.) → เหลือ 96 ✓ แต่ไปกดเบิกมืออีก 20 กก. → เหลือ 76
   ทั้งที่ใช้จริงแค่ 4 กก.

ตรวจข้อมูลจริงเพิ่ม: รับเข้าใย 188 รายการ **ไม่มีรายการไหนกรอก "จำนวนก้อน" ไว้เลย**
ระบบจึงคำนวณน้ำหนักต่อก้อนเองไม่ได้ — เลยยึด **เบอร์ล็อตในสต๊อก** เป็นตัวแทน "ก้อน" ซึ่งมียอดคงเหลือจริงอยู่แล้ว

## แก้เป็นแบบใหม่

### 1. แถวสูตรมี 3 ช่อง
| ช่อง | คือ |
|---|---|
| ① ชื่อใย | ดรอปดาวน์ **ใยที่มีอยู่จริงในแอพสต๊อก** (เดิมมีแต่ชื่อที่เคยพิมพ์ในออเดอร์) หรือพิมพ์ชื่อใหม่ |
| ② เบอร์ก้อน | ดรอปดาวน์ก้อนที่ยังมีของ พร้อมยอด `ก้อน/ล็อต 2 — เหลือ 20 กก.` หรือเลือก "+ พิมพ์เบอร์เอง" |
| ③ กก./รอบ | เท่าเดิม |

เหลือก้อนเดียวเลือกให้เลย ไม่ต้องกดซ้ำ
ใต้แถวบอกว่า `ใช้ก้อน/ล็อต 1 เหลือ 6 กก.` และเตือนถ้าพิมพ์เบอร์ที่ไม่มีในสต๊อก

### 2. ก้อนหมด → สลับให้เองทันที ไม่ต้องรอคน
หักจากก้อนที่เลือกไว้ในสูตรก่อนเสมอ พอหมดก็ไหลไปก้อนถัดไป **ของใยชื่อเดียวกัน** ต่อในรอบเดียวกันเลย
แล้ว**ย้ายสูตรไปชี้ก้อนใหม่ให้เอง** — รอบถัดไปเริ่มจากก้อนใหม่ทันที

Toast บอกทันที
`รอบที่ 2 — 7D64HS เหลือ 38 กก. · LM เหลือ 48 กก. 🔄 7D64HS: ก้อน 1 หมด → ก้อน 2`

### 3. ประวัติการเปลี่ยนก้อน
ตารางใหม่ในหน้าป้อนใย เก็บทุกครั้งที่เปลี่ยนก้อน ทั้งที่ระบบสลับเองและที่คนกดเปลี่ยน

| เวลา | ใย | เปลี่ยนจาก | เป็นก้อน | เหลือในก้อนใหม่ |
|---|---|---|---|---|
| 2026-08-21 08:02 | 7D64HS | ก้อน 1 **หมดแล้ว** | ก้อน 2 | 20 กก. |

### 4. กันยอดหักซ้ำ
- การ์ด "เบิกใย (เปลี่ยนก้อนใย)" เปลี่ยนชื่อเป็น **"เบิกใยเพิ่มเอง (ใช้เฉพาะกรณีพิเศษ)"**
  เพราะการเปลี่ยนก้อนย้ายไปทำที่สูตร/การ์ดก้อนใยแล้ว
- ขึ้นแถบเตือนบนการ์ด: *ระบบหักใยให้อัตโนมัติทุกรอบป้อนอยู่แล้ว — เบิกตรงนี้เพิ่มยอดจะถูกหักซ้ำ*
- กดบันทึกยังถามยืนยันอีกชั้น พร้อมบอกว่าจะหักเพิ่มกี่กิโล
- ปิดหักอัตโนมัติเมื่อไหร่ ข้อความเปลี่ยนเป็น *ต้องเบิกใยเองทุกครั้งที่เปลี่ยนก้อน*

### 5. ปุ่ม 🔄 เปลี่ยนก้อน (กรณีอยากข้ามก้อนเอง)
บอกก้อนที่ยังมีของทั้งหมดให้เลือก `#1 (6 กก.), #2 (20 กก.), #3 (20 กก.)` เดาก้อนถัดไปให้
พิมพ์เบอร์ที่ไม่มีในสต๊อกได้ แต่ถามยืนยันก่อน — และบันทึกลงประวัติเหมือนกัน

## ผลทดสอบ (20/20)
| # | ทดสอบ | ผล |
|---|---|---|
| 1-3 | แถวสูตรมี 3 ช่อง · ชื่อใยดึงจากสต๊อก · ยังไม่เลือกชื่อบอกให้เลือกก่อน | ✓ |
| 4-5 | เบอร์ก้อนขึ้นพร้อมยอดคงเหลือ + พิมพ์เองได้ · ใต้แถวบอกก้อนที่ใช้อยู่ | ✓ |
| 6 | บันทึกสูตรเก็บเบอร์ก้อนไว้ด้วย | ✓ |
| 7 | ป้อน 1 รอบ หักจากก้อนที่เลือกไว้ | ✓ |
| 8-9 | ก้อน 1 หมด (6 กก.) ไหลไปก้อน 2 ในรอบเดียวกัน + สูตรย้ายตาม | ✓ |
| 10-11 | Toast บอกว่าเปลี่ยนก้อน · ใยตัวอื่นไม่กระทบ | ✓ |
| 12-13 | ประวัติขึ้นครบ ใย/ก้อนเก่าหมด/ก้อนใหม่/ยอดคงเหลือ | ✓ |
| 14 | การ์ดโชว์ก้อนที่ใช้อยู่ + ก้อนถัดไป | ✓ |
| 15 | การ์ดเบิกมือเตือนหักซ้ำ | ✓ |
| 16-17 | ลบรอบป้อนใย → บอกก่อนแล้วคืนใยจริง | ✓ |
| 18-19 | ปิดหักอัตโนมัติ → ไม่หักสต๊อก + ข้อความเตือนเปลี่ยน | ✓ |
| 20 | ไม่มี JavaScript error | ✓ |

รันชุดเทสเดิมซ้ำทั้งหมด ผ่านหมด — **รวม 118 เคส**
เวอร์ชันที่มีล็อกอินอัปเดตตามด้วย (เทสล็อกอิน 20/20 ผ่านเหมือนเดิม)

---

# รอบที่ 10 — เบอร์ก้อน = เบอร์ล็อต แสดงเหมือนที่เขียนไว้บนก้อนจริง
ไฟล์ที่แก้: `factory-app-supabase.html` + `stock-app_1.html`

## ยืนยันความเข้าใจให้ตรงกัน
**เบอร์ก้อน = เบอร์ล็อต เป็นอันเดียวกัน** และ **รับเข้า 1 ครั้ง = 1 ก้อน**
ใยชื่อเดียวกันแต่ละก้อนหนักไม่เท่ากัน ระบบจึงต้องแยกยอดทีละก้อน — ซึ่งทำได้อยู่แล้วเพราะแต่ละล็อตมีน้ำหนักของตัวเอง

รูปแบบที่เขียนบนก้อนจริง: `เบอร์ 1 · 7D64HC · 265 กก. · 21/8/2569`

## แก้ให้แสดงตรงกับที่เขียนบนก้อน

### แอพผลิต
- **ดรอปดาวน์เบอร์ก้อนในสูตร** — `เบอร์ 2 · รับเข้า 265 กก. (21/8/2569) · เหลือ 180 กก.`
  เห็นครบทั้งเบอร์ น้ำหนักตอนรับเข้า วันที่ และยอดที่เหลือจริง ไม่ต้องเปิดแอพสต๊อกเทียบ
- **ใต้แถวสูตร** — `ใช้ก้อน เบอร์ 1 · 7D64HS 6 กก. · 21/8/2569 — เหลือ 6 กก.`
- **การ์ดก้อนใย** — ป้ายก้อนเต็มรูปแบบ + ก้อนถัดไปพร้อมน้ำหนัก `ก้อนถัดไป เบอร์ 3 (180 กก. เหลือ 180 กก.)`
- **กล่องเปลี่ยนก้อน** — ไล่ก้อนที่ยังมีของเป็นรายการ พร้อมป้ายเต็มทีละบรรทัด
- **Toast** — `🔄 7D64HS: ก้อน 1 หมด → ก้อน 2 (265 กก.)`
- **ประวัติการเปลี่ยนก้อน** เพิ่ม 2 คอลัมน์

| เวลา | ใย | ก้อนเดิม | เริ่มก้อนใหม่ | รับเข้า | วันที่รับเข้า | เหลือในก้อนใหม่ |
|---|---|---|---|---|---|---|
| 21/8 08:24 | 7D64HS | เบอร์ 1 หมดแล้ว | เบอร์ 2 | 265 กก. | 21/8/2569 | 265 กก. |

- วันที่แสดงเป็น พ.ศ. แบบที่เขียนบนก้อน (21/8/2569) ไม่ใช่ ค.ศ.

### แอพสต๊อก — ฟอร์มรับเข้าใย
- หัวข้อเปลี่ยนเป็น **"รับเข้าใย — 1 ครั้ง = 1 ก้อน"** พร้อมตัวอย่างตรงตามที่เขียนบนก้อน
  และหมายเหตุว่า *ใยชื่อเดียวกันแต่ละก้อนหนักไม่เท่ากันได้ ระบบแยกยอดให้ทีละก้อน*
- `เลขล็อต (ถ้ามีเขียนไว้อยู่แล้ว)` → **`เบอร์ก้อน — เลขที่เขียนไว้บนก้อน`**
- `น้ำหนักรับเข้า (กก.)` → **`น้ำหนักก้อนนี้ (กก.)`** ตัวอย่าง 265
- ช่องจำนวนก้อนอธิบายใหม่ว่าปกติเว้นว่าง ใช้เฉพาะตอนรับหลายก้อนรวดโดยไม่แยกเบอร์
- เตือนเบอร์ซ้ำชัดขึ้น: *ระบบจะมีก้อนเบอร์ 3 ซ้ำกัน 2 ก้อน ทำให้ยอดคงเหลือดูสับสน*

## ผลทดสอบ (20/20)
ใช้ข้อมูลที่ก้อนหนักไม่เท่ากันจริง — เบอร์ 1 = 6 กก., เบอร์ 2 = 265 กก., เบอร์ 3 = 180 กก.

| # | ทดสอบ | ผล |
|---|---|---|
| 4 | ดรอปดาวน์ขึ้น `เบอร์ 2 · รับเข้า 265 กก. (21/8/2569) · เหลือ 265 กก.` | ✓ |
| 5 | ใต้แถวขึ้นป้ายก้อนเต็มรูปแบบ | ✓ |
| 8-9 | เบอร์ 1 หมด (6 กก.) ไหลไปเบอร์ 2 ในรอบเดียวกัน + สูตรย้ายตาม | ✓ |
| 10 | Toast บอกน้ำหนักก้อนใหม่ 265 กก. | ✓ |
| 12 | ประวัติมีน้ำหนักรับเข้าและวันที่ พ.ศ. ครบ | ✓ |
| 14 | การ์ดโชว์ป้ายก้อน + ก้อนถัดไปพร้อมน้ำหนัก | ✓ |
| อื่นๆ | เหมือนรอบก่อนทั้งหมด | ✓ |

รันชุดเทสเดิมซ้ำทั้งหมด ผ่านหมด — **รวม 118 เคส** · เวอร์ชันมีล็อกอินอัปเดตตามแล้ว
